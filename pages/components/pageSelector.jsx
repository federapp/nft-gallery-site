export const PageSelector = ({propToPass}) => {
    
    return (
        <div className="w-1/4 flex flex-col">
            <h1>Here goes the page selector {`${propToPass}`}</h1>
        </div>
    )
}
